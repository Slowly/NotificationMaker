<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Used in the AddActivity to make the button bigger -->
    <style name="StyledButton" parent="Widget.AppCompat.Button.Colored">
        <item name="android:minHeight">55dp</item>
    </style>

    <!-- Tabs in the MainActivity-->
    <style name="AppTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabBackground">@color/colorPrimaryLight</item>
        <item name="tabSelectedTextColor">@color/white</item>
    </style>

    <!-- Tabs, the style for the text in the tabs -->
    <style name="AppTabLayout.Text" parent="TextAppearance.Design.Tab">
        <item name="android:textColor">@color/lightgray</item>
    </style>

</resources>
